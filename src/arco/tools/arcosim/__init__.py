"""arcosim — ARCO simulation platform.

Provides the ``arcosim`` CLI entry point for running ARCO simulation scenarios
from YAML scenario files.  Requires both the ``tools`` and ``pygame`` optional
dependency groups::

    pip install arco[tools,pygame]
"""
